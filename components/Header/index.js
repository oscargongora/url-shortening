import Logo from "../Logo";
import Nav from "../Nav";
import Hamburger from "../Hamburger";

const Header = () => {
  return (
    <header>
      <Logo fill="#35323D"></Logo>
      <Nav></Nav>
      <Hamburger></Hamburger>
    </header>
  );
};

export default Header;
